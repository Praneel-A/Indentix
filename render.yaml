services:
  - type: web
    name: indentix-api
    runtime: node
    rootDir: apps/api
    buildCommand: npm install --ignore-scripts && npx tsc
    startCommand: node dist/index.js
    envVars:
      - key: PORT
        value: 4000
